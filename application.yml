eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVICE_FULL_URL:http://localhost:8761/eureka}
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: eureka-0
    prefer-ip-address: true


security:
  oauth2:
    client:
      registration:
        customer-service:
          client-id: customer-service-client
          client-secret: YXb8JXTgN3XZMPq4zGR5acasX9IHAnMmVPW6hXOcrwg
        business-service:
          client-id: business-service-client
          client-secret: Bq7dPpZ7qbQFABoURxkFXhQwFyBz7OCkqyeDauZ670g
        payment-service:
          client-id: payment-service-client
          client-secret: KxY9wTHbQVj6p36SRygZ64s5SivkjeHAa98r7FjFOO8
spring:
  output:
    ansi:
      enabled: always
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  # Kafka Producer Configuration
  kafka:
    bootstrap-servers: ${KAFKA_SERVICE:kafka-service:9092}
    producer:
      bootstrap-servers: ${KAFKA_SERVICE:kafka-service:9092}
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.ByteArraySerializer

    # Kafka Consumer Configuration
    consumer:
      bootstrap-servers: ${KAFKA_SERVICE:kafka-service:9092}
      group-id: user-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer  
  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
  health:
    readiness-state:
      enabled: true

# OAuth2 resource config
oauth2:
  google:
    api:
      verifier: ${GOOGLE_VERIFIER:https://www.googleapis.com/oauth2/v3/certs}
      issuer: ${GOOGLE_ISSUER:https://accounts.google.com}
  apple:
    api:
      verifier: ${APPLE_VERIFIER:https://appleid.apple.com/auth/keys}
      issuer: ${APPLE_ISSUER:https://appleid.apple.com}
  wtdnow:
    api:
      verifier: ${WTD_NOW_VERIFIER:http://security-server-svc/oauth2/jwks} # Todo: tobe fixed should change with security service
      issuer: ${WTD_NOW_ISSUER:https://it.wtdnow.com}
REDIS_HOST: redis
REDIS_PORT: 6379      
logging:
  level:
    org:
      springframework:
        security: TRACE

